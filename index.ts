



/*
session
    post            // crea una session

users
    get             // trae todos los usuarios
    get{id}         // trae un usuario por id
    post            // crea un usuario
    put{id}         // modifica un usuario por id
    delete{id}      // elimina un usuario por id


client
    get             // trae todos los clientes
    get{id}         // trae un cliente por id
    post            // crea un cliente
    put{id}         // modifica un cliente por id
    delete{id}      // elimina un cliente por id


product (sub {item})
    get             // trae todos los productos
    get{id}         // trae un producto por id
    post            // crea un producto
    put{id}         // modifica un producto por id
    delete{id}      // elimina un producto por id


sale (sub {bill})
    get             // trae todas las ventas
    get{id}         // trae una venta por id
    post            // crea una venta
    put{id}         // modifica una venta por id
    delete{id}      // elimina una venta por id
*/